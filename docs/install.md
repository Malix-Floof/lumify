# Установка

Установка из PyPI:
```
pip install -U lumify
```
Установка напрямую из Git:
```
pip install git+https://github.com/Malix-Floof/lumify.git
```
Установка напрямую из Git с указанием ветки:
```
pip install git+https://github.com/Malix-Floof/lumify.git@main
```
